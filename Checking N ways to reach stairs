// Checking N ways to reach stairs
using namespace std;
int fab(int n)
{
    if(n==1 || n ==2)
    return n;

    
    return fab(n-1)+fab(n-2);
    
}
#include <iostream>

int main() {
    cout<<fab(4);
  

    return 0;
}
